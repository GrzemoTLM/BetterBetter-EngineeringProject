# --- Postgres (used by docker-compose and Django) ---
POSTGRES_USER=grzegorz
POSTGRES_PASSWORD=admin
POSTGRES_DB=betbetter_db

# --- pgAdmin (used by docker-compose) ---
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

# --- Django DB settings (used by settings.py) ---
DB_NAME=betbetter_db
DB_USER=user
POSTGRES_PASSWORD=userpassword
DB_HOST=localhost
DB_PORT=5432

# If you run Django inside a container (not in this compose), set DB_HOST=db
# DB_HOST=db

# --- Django core ---
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000

# --- Third-party integrations (optional) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
MAILERSEND_API_KEY=

